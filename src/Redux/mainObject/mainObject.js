export const generateObjectMain = (
  cityKey,
  cityName,
  countryName,
  weatherCurrObject,
  weatherFiveObject
) => {
  return {
    cityKey,
    cityName,
    countryName,
    weatherCurrObject,
    weatherFiveObject,
  };
};
